# 飞书 Wiki 导出

先登录，再粘贴任意目标 Wiki 页面地址并读取目录。勾选范围后开始导出，插件会保留 Wiki 层级、Markdown 正文和可下载图片，并用 SQLite checkpoint 精准记录进度。

如果已在“飞书导入”中保存 App ID 和 App Secret，原生飞书文档会优先通过官方文档块 API 读取完整内容和图片，避免超长文档受到网页虚拟列表、滚动和懒加载的影响；未配置凭证、接口不可用或文档包含暂未支持的内容时，会自动回退到原有浏览器采集方式。
