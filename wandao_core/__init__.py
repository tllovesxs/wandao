"""Versioned, platform-neutral runtime services for Wandao Plugin v1.

Plugin backends should depend on these services rather than another platform's
implementation. Legacy top-level ``wandao_*`` modules remain import-compatible
facades during the migration.
"""
